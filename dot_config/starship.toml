command_timeout=750

format = '''[->](bold green) $directory$aws$git_branch$git_commit$git_state$git_metrics$git_status$terraform(bold green) 
$character'''

[directory]
style='cyan'
truncation_length = 8

[direnv]
disabled=false
detect_files=['.env']

