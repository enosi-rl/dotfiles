# Don't let ripgrep vomit really long lines to my terminal, and show a preview.
--max-columns=750
--max-columns-preview

# Using glob patterns to include/exclude files or folders
--glob=!vendor/

# Disable ignore case if search contains uppercase
--smart-case

--type-add=tf:*.tfvars
--type-add=got:*_test.go
